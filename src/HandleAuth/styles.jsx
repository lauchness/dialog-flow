import styled from "@emotion/styled";
import { font, color } from "../theme";

export const HandleAuthContainer = styled.div`
  height: 100%;
  min-height: 100vh;
  width: 100%;
  display: flex;
  justify-content: center;
  align-items: center;
  color: ${color.white};
  font-size: ${font.size.large};
`;
